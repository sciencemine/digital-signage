<div id="properties-panel" data-spy="scroll" data-target="#propertiesNav" class="panel panel-info {{if expanded "properties-panel--scrollable"}}">
  <div {{action 'toggleView' on click}} class="properties-panel--header panel-heading">
    <h4 class="panel-title">Properties <small>Scroll me<br>Click to {{if expanded "hide" "show"}} <span class="glyphicon glyphicon-chevron-{{if expanded 'up' 'down'}}" /></small></h4>
  </div>

  {{#if expanded}}
    {{#if data}}
      <div class="panel-body">
        <p>List of properties the video has.</p>
      </div>

      <nav class="properties-navbar navbar">
        <div class="collapse navbar-collapse" id="propertiesNav">
          <ul class="nav navbar-nav">
            {{#each-in data as |key value|}}
              <li data-toggle="propertiesTooltip" title={{get-display-name config.videos.data.video.data key}}><a href="#{{key}}_label"><span class="glyphicon glyphicon-picture"/></a></li>
            {{/each-in}}
          </ul>
        </div>
      </nav>

      <div class="panel-body">
        {{generic-form showList=true showHelp=true showTitle=false relations=relations attributes=attributes updateModalCallback=updateModalCallback editRelationCallback=editRelationCallback class="properties-panel__form" noLabel=false data=data config=config.videos.data.video}}
      </div>
    {{/if}}
  {{/if}}
</div>
